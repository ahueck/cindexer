from setuptools import setup, find_packages

setup(
    name="cindexer-python-ipc",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    description="A library for communicating with the CIndexer VS Code extension",
    author="cindexer",
)